# Progress

## Шкала

- 0 — не встречал
- 1 — узнаю концепцию
- 2 — могу объяснить с помощью
- 3 — могу реализовать с помощью
- 4 — могу реализовать самостоятельно
- 5 — могу объяснить, реализовать, отладить и сравнить альтернативы

Оценка консервативна: код в репозитории показывает контакт с концепцией, но не всегда авторство или самостоятельность.
Уровни 4–5 не выставлены без независимой реализации и объяснения trade-offs.

## Текущая фаза

**Урок 4 — E4.5, логическое удаление.**

## Текущая задача

**Метаданные таблицы проверены; логическое удаление и фильтрация чтений ещё не подтверждены.**

2026-09-21: пользователь добавил необязательные настройки `table` с именем колонки
через `keyof Cols` и сохранил их в `_options`. С помощью разделил конкретный `TableDef`
и структурный `AnyTableDef`, затем исправил несовместимость явно записанного `undefined`.
`pnpm check-types` прошёл после правки схемы. Временная проверка реальных исходников
подтвердила сохранение имени `removedAt`, вызов без настроек, сохранность метаданных колонок,
совместимость общего типа, сохранение конкретной схемы и запрет опечатки `deletedOn`.
Временные файлы удалены; SQL в проверке метаданных не выполнялся.
Пользователь затем восстановил обычный тип аргумента `delete` и предложил статический отбор
подходящих колонок. Написал `SoftDeleteKeyName`, после подсказки исправил обратную проверку PK.
`pnpm check-types` прошёл. Временная проверка формулы подтвердила исключение PK,
non-nullable timestamp и nullable text, точный результат для одной или двух подходящих
колонок, `never` без подходящих колонок и работу с настоящими конструкторами схемы.
Пользователь подключил отбор к `TableDefOptions`. Изолированная проверка реального
table(...) подтвердила разрешение подходящей колонки и вызова без настроек, запреты
nullable text, non-nullable timestamp, PK и неизвестного имени. Временные файлы удалены.
Пользователь заменил ограничение InferUpdate на AnyTableDef. Изолированная проверка
реальных построителей UPDATE/SELECT подтвердила сохранение конкретных ключей и типов,
запрет неверных значений, неизвестных колонок и изменения PK. Временные файлы удалены.
Пользователь подключил новую функцию к публичному delete и добавил invalidate после
успешного update. `pnpm check-types` прошёл. Временный SQLite-сценарий через repo.delete
подтвердил сохранение строки с датой и очистку карты в логической ветке, физическое удаление
без настройки, сохранность соседней строки/объекта и сохранность целевой строки/объекта
при отказе UPDATE через SQL-trigger. Файлов сценария не создавали.
После исправления опций прошли check-types и изолированная проверка вызова delete
с expectedVersion. SQLite подтвердил OptimisticLockError и неизменность строки/карты
при неверной версии, запись даты, увеличение версии и очистку карты при верной версии.
Временные проверочные файлы удалены. Это проверка логической ветки; физическая ветка
expectedVersion не использует. По просьбе пользователя фильтрация отложена: сначала NOW().
Фильтрация чтений и весь E4.5 ещё не завершены; постоянных тестов soft delete пока нет.
Результат достигнут с помощью; уровни освоения не повышены. Продолжение — в `session.md`.

Пользователь добавил NowExpr с единственным тегом Now в объединение Expr.
`pnpm check-types` обнаружил необработанный вариант в compileExpr и printExpr
на Match.exhaustive. Поддержка времени базы ещё не реализована; удаление пока использует
new Date(). Пользователь добавил печать Now; временный вызов prettyPrint подтвердил
NOW() без кавычек и отличие от строкового Literal. Затем пользователь добавил
компиляцию Now для PostgreSQL/SQLite и исправил пропущенную запятую; check-types прошёл.
Временный сценарий подтвердил точные SQL и параметры одного UPDATE IR на обоих диалектах,
исполнение на SQLite и PGlite, дату в целевой строке и сохранность соседней строки.
SQLite вернул UTC ISO-строку; дата PGlite совпала с NOW() начала транзакции.
Это проверка IR/compiler, не поддержки выражения через builder или Repository.
Пользователь добавил now(): Expr<Date> без приведения типа; check-types прошёл.
Временный вызов now() и compile подтвердил узел Now, SQL обоих диалектов и отсутствие
параметра даты. Это проверка конструктора и компилятора, не передачи через .set(...).
UPDATE пока оборачивает все входные значения в Literal; следующий шаг — эта граница.
Пользователь добавил общий реальный Symbol ExprTypeId и свойство в now(), после диагностики
различия объявления типа и создания объекта. Check-types и runtime-проверка подтвердили
отличие now() от JSON с тем же _tag и сохранение компиляции для обоих диалектов.
Пользователь добавил маркер в типизированные lit/col и сделал его обязательным для
LiteralExpr/ColumnExpr. Runtime-проверка всех трёх конструкторов прошла: JSON внутри
lit не изменён, ссылка на колонку сохранена. Затем пользователь добавил маркер в базовые
col/lit и ExpressionBuilder.col, подключил b.now(). Runtime-проверка пяти путей прошла.
Check-types оставляет девять ошибок маркера в statements/write-builders.
Пользователь исправил потерю Date в интерфейсе b.now(). Прежняя изолированная проверка
совместимости прямого now() и b.now() с Expr<Date> прошла; временные файлы удалены.
Пользователь перевёл statements и три RETURNING в write-builders на базовые конструкторы.
Check-types прошёл. Четыре файла проверок запросов/компилятора: 34 теста прошли, один round trip
упал на сравнении с structuredClone, теряющим Symbol-маркер. Отличие копии до execute
подтверждено отдельно без SQL. Пользователь заменил сравнение на проверку Literal active
с value: true через toMatchObject. Целевой round trip и check-types прошли; повторный
запуск четырёх файлов — 35/35. Проверка сохраняет инвариант доменного значения INSERT,
а не сравнение всех полей IR. Следующий шаг — распознавание выражений через isExpr.

### История E4.4

2026-09-20: пользователь верно предсказал отсутствие изменений при повторной записи со старой
версией и написал SQL с проверкой `id`/`version` и увеличением `version` в том же UPDATE.
Временный сценарий наставника на SQLite подтвердил: первая запись изменила одну строку,
повторная со старой версией — ноль; сохранились имя первой записи и версия `4`.
Проверка выполнена через `better-sqlite3`, не через Repository. Файлы не создавались.
Массив параметров подготовил наставник; SQL пользователь написал после объяснения.
Уровни освоения не повышены. Следующий подшаг указан в [session.md](session.md).

Пользователь добавил `OptimisticLockError` и после диагностики исправил скопированный тег
`QueryInvariantError`. Временная проверка экземпляра подтвердила тег `OptimisticLockError`;
`pnpm check-types` прошёл. Ошибка не включена в `DriverError`.
После объяснения источника ожидаемой версии пользователь верно выбрал `OptimisticLockError`
для ожидаемой версии `3` при сохранённой версии `4`. Это ответ с помощью;
самостоятельное объяснение механизма ещё не подтверждено.
Пользователь подготовил SQLite-сценарий `expected version` и добавил третий аргумент
`{ expectedVersion: 1 }` к `update`. После исправления DDL сценарий без утверждений проходил,
`pnpm check-types` прошёл. Пользователь добавил точное сравнение результата первого update.
Первый целевой запуск подтвердил отсутствие увеличения версии. Пользователь подключил
expectedVersion к SET и WHERE; текущий тест проходит (1 тест, 14 пропущены).
Проверку метаданных пользователь исправлял с помощью: сначала проверял объект ColumnDef
как число, затем молча отключал сравнение версии при несовместимой схеме.
После переноса проверки до SQL временный SQLite-сценарий подтвердил QueryInvariantError
как дефект и неизменность строки для пяти схем: отсутствующая колонка, text, nullable,
PK и integer с кодеком. Обычный UPDATE без expectedVersion для таблицы без версии прошёл.
`pnpm check-types` прошёл. Проверки выполнены наставником; файлы сценариев не создавались.
Пользователь перенёс приведение на выражение колонки и после уточнения указал `Expr<number>`.
Повторно прошли `pnpm check-types` и целевой тест (1 тест, 14 пропущены).
Пользователь добавил проверку повторной записи со старой версией. Целевой тест обнаружил
NotFoundError вместо OptimisticLockError (1 тест упал, 14 пропущены).
Пользователь разделил пустой результат UPDATE по наличию expectedVersion.
Сценарий конфликта и существующий обычный update/delete прошли: 2 теста, 13 пропущены;
`pnpm check-types` прошёл. Пользователь убрал QueryInvariantError из канала E, сохранив
Effect.die. После правки check-types и оба сценария обновления прошли повторно.
Пользователь добавил чтение через findBy после конфликта: имя Boris и версия 2 сохранены.
Весь файл make-repository.test.ts прошёл (15 тестов), `pnpm check-types` прошёл.
Основной сценарий E4.4 на SQLite подтверждён. После объяснения двух чтений одной версии
пользователь верно предсказал отсутствие конфликта при передаче новой прочитанной версии.
Учебный цикл завершён с помощью; самостоятельное объяснение всего механизма не подтверждено,
уровень освоения не повышен.
Ограничения и результат — [запись 0046](records/0046-optimistic-update-conflict.md).

### Историческая точка: INSERT и кодирование WHERE

Пользователь завершил подготовку копии INSERT, декодирование RETURNING и сохранение доменного
типа JSON через `withCodec` → `json<T>()` → `InferValue` → `InferColumn`.
Постоянные тесты подтверждают boolean round-trip, сохранение `null`, неизменность исходного IR,
типизированную ошибку кодека без записи, boolean и JSON в результате `repo.save`.
Общий `AnyCodec` используется для хранения; локальные приведения вызываемых функций в исполнителе
остаются непроверенной границей. Runtime-валидация не добавлена.

Последний запуск трёх query test-файлов — 34 теста прошли. После удаления `as never` из `withCodec`
и неиспользуемого импорта `Codec` прошёл `pnpm check-types`; тесты после очистки не перезапускались.
Следующий пользовательский тест — `repo.findBy({ active: true })` с записью через `repo.save`.
Тест ещё не написан; отсутствие encode в WHERE установлено по исходнику, новый отказ не запускался.
UPDATE и остальные формы условий остаются открытыми. Подробная точка продолжения — в
[session.md](session.md), разделе «Продолжить отсюда».

Самостоятельное восстановление типов и исполнителя не подтверждено; реализация написана пользователем
с подсказками. Уровни освоения по одному успешному набору тестов не повышены.

### История L3.29 до возобновления

2026-09-14: после объяснения пользователь верно выбрал `isEnabled` как ключ фабрики
для переименованного поля результата. До объяснения перепутал encode/decode и не восстановил
поиск метаданных колонки. Полная самостоятельная цепочка E3.5 и предложенная мутация
не подтверждены; уровень освоения не повышен.
Повторная проверка `pnpm test src/query/builder.test.ts` — 14 тестов прошли.
Пользователь написал тест `round trip` через INSERT и SELECT builders на SQLite.
По переданному выводу SQLite получил `params: [true]` и отказал при привязке параметра;
TypeError обёрнут драйвером в DbError. Тест прочитан, повторно ради подтверждения ошибки
не запускался. Это подтверждение отсутствия encode на записи, не успешного round-trip.
Пользователь добавил runtime-таблицу в конструктор ExecutableInsert и передал её в
values и returning. `pnpm check-types` прошёл. Временный сценарий Node/tsx подтвердил
сохранение объекта таблицы в обоих запросах, поиск фабрики, прямой encode(true) в 1
для SQLite и true для PostgreSQL, сохранение доменного true в IR и returning: null
у исходного запроса. Сценарий читал поля объектов из JavaScript, SQL не выполнялся,
файлы не создавались. Автоматическое кодирование при исполнении не реализовано.
Пользователь верно объяснил получение одного Driver из предоставленного Layer.
Вместо позиционных параметров выбрал RunOptions, передал table из ExecutableInsert
через run в runWithSql и мигрировал существующие вызовы.
`pnpm check-types` прошёл; builder.test.ts и make-repository.test.ts — 21 тест прошёл.
Пользователь собрал словарь кодеков колонок INSERT после получения Driver.
`pnpm check-types` прошёл. Временный Node/tsx-сценарий с подменённым Driver подтвердил
отложенный вызов фабрики, один вызов на две строки за исполнение, выбор предоставленного
диалекта и пропуск колонки без кодека. encode ещё не вызывается, boolean-параметры
не изменены. SQL не выполнялся, файлы не создавались. Round-trip остаётся незавершённым.
Пользователь добавил отдельный массив поверхностных копий строк IR.
`pnpm check-types` прошёл. По исходнику массив пока не подключён к compile;
новое SQL-поведение этим этапом не проверялось.
Пользователь добавил encode через Effect.try и CodecError; `pnpm check-types` прошёл.
Сценарий с подменённым Driver подтвердил ошибочное кодирование null в 0.
Отдельная проверка текущего выражения с optional chaining дала Literal(undefined)
при отсутствии кодека. По исходнику push сохраняет необработанную копию row.
Пользователь исправил пропуск null и колонок без кодека, убрал optional chaining,
сохранил в массив именно encodedRow. `pnpm check-types` прошёл.
Сценарий с подменённым Driver подтвердил encode для true/false, пропуск null,
сохранность замороженных исходных строк и выражений. SQL не выполнялся.
Пользователь ввёл preparedStm и передал её в compile; `pnpm check-types` прошёл.
Переменная пока ссылается на stmt. Следующий подшаг — присвоить ей копию INSERT
с encodedRows и проверить исходный round-trip на SQLite. Round-trip не завершён.

### История E3.5

Реализация и семь постоянных тестов завершены; самостоятельное воспроизведение пользователем ещё не подтверждено.

Базовая линия перепроверена запуском на SQLite: проекция над колонкой `bool()` вернула `1` и `0`
при `typeof === 'number'`, тогда как тип результата обещает `boolean`. Далее — история E3.4,
долги которого остаются открытыми.

Пользователь добавил `BuilderState.sources` и регистрацию объекта таблицы в `selectFrom`
после объяснения и указания формы словаря. `pnpm check-types` — код `0`.
Временный сценарий Node/tsx подтвердил сохранение объекта таблицы после `limit`/`select`
и поиск кодека по выражению переименованной колонки; прямое декодирование `1`/`0`
вернуло `true`/`false`. Декодирование через `execute` ещё не реализовано.
Самостоятельное проектирование связи проекции с метаданными пока не подтверждено.

Пользователь собрал фабрики в `select` и `selectAll` с подсказками. `pnpm check-types`
прошёл. Временный сценарий Node/tsx подтвердил имя `active` для `selectAll`, имя
`enabled` для переименованной колонки в `select`, пропуск литерала перед колонкой с
кодеком и integer-колонки без кодека. Проверены ссылки на исходные фабрики.
Пользователь заменил `cols ?? {}` явной проверкой с синхронным `QueryInvariantError`
и исключил `codecFactories` из IR. `pnpm check-types` прошёл. Повторный временный
сценарий подтвердил исключение для отсутствующего источника, фабрику обычного `selectAll`
и отсутствие `sources`/`codecFactories` в IR обоих методов выбора колонок.
Декодирование при исполнении не реализовано; SQL в этих сценариях не выполнялся.

Пользователь сформулировал сохранение `null` без вызова декодера и предложил необязательный
параметр исполнителя. Передача фабрик из трёх методов `ExecutableQuery` через `run` в
`runWithSql` подтверждена чтением исходников. Порядок применения `NonNullable` к типу
`ColumnDef['_codec']` исправлен после объяснения. `pnpm check-types` прошёл.
Декодирование строк ещё не реализовано; новая обработка результатов не проверялась.

Пользователь реализовал вызов декодера через `Effect.try` и `CodecError` с подробными
подсказками. `pnpm check-types` прошёл. Временные сценарии SQLite подтвердили
`select`/`selectAll`, переименование, все три метода исполнения, `false`/`true`/`null`,
отсутствующую строку LEFT JOIN, пустые результаты, поля без кодеков, успешный JSON
и типизированную ошибку некорректного JSON с исходными значением и причиной.
Фабрика выбиралась один раз для трёх строк; `null` не передавался декодеру.
PGlite подтвердил выбор диалекта `postgres` и нативные boolean/NULL.
На этапе временных проверок три query test-файла — 22 теста прошли.
Файлы временных сценариев не создавались. Самостоятельное решение E3.5 не подтверждено.
Пользователь исключил копирование строк и создание `decodedRows` при пустом списке
кодеков. Проверка с подменённым Driver подтвердила отсутствие копирования без кодеков
и неизменность исходных строк при декодировании. После последней правки проверка типов,
22 существующих теста и полный временный сценарий SQLite прошли повторно.
После явного отключения наставничества агент добавил семь постоянных тестов в
`src/query/builder.test.ts`. Они проверяют проекции, все методы исполнения,
INNER/LEFT self JOIN, boolean/NULL/JSON, поля без кодеков, ошибки декодирования,
выбор SQLite/PostgreSQL при повторном использовании запроса и сохранность строк Driver.
Все 12 мутаций обнаружены; мутации не изменяли рабочие исходники, временная конфигурация удалена.
Полный `pnpm test` — 91 тест в 21 файле прошёл. `pnpm check-types` прошёл.
Самостоятельное воспроизведение остаётся непроверенным; авторство тестов принадлежит агенту.

Пользователь перешёл к E3.4; долги L3.27–L3.28 не закрыты автоматически.
В `src/query/make-repository.ts` поиск по частичному объекту условий возвращает строку либо `null`.
`pnpm check-types` — код `0`. Временный сценарий на SQLite подтвердил совпадение по двум полям,
`null` при отсутствии строки, независимость последовательных вызовов и сохранение ошибки `DbError`.
Преобразование через `Effect.map(Option.getOrNull)` применено с подсказкой; самостоятельность не подтверждена.
Пользователь реализовал `findMany`: `selectAll().execute()` и публичный метод фабрики.
`pnpm check-types` и временный сценарий на SQLite прошли: две совпадающие строки, пустой массив без совпадений,
одна строка по двум условиям; предыдущие сценарии `findBy` также прошли. Порядок строк не фиксировался.
Пользователь реализовал `PrimaryKeyName<T>` с помощью: условие `_pk`, отображение в имя `K` и индексирование
по ключам колонок. `pnpm check-types` прошёл; дополнительная проверка compiler API TypeScript 6.0.3
на виртуальном исходнике подтвердила `"id"` и `"externalId"` для двух схем. Самостоятельность не подтверждена.
Пользователь завершил `findById` с типом аргумента `InferRow<T>[PrimaryKeyName<T>]`.
Объяснил нежелательный `undefined` от `Partial`. Ограничение ключей через `& string` позволило пройти
`pnpm check-types` без утверждения типа для `pk`; ранее предложенное утверждение оказалось ненужным.
SQLite-сценарий подтвердил поиск по числовому и текстовому ключу с разными именами колонок,
`null` при отсутствии строки, независимость вызовов и сохранение `DbError`.
Схема без ключа вызывает `PrimaryKeyError` при создании репозитория.
Дополнительные виртуальные проверки TypeScript 6.0.3 подтвердили запрет неверного типа ключа и `undefined`.
Повторная проверка текущего `findById` на SQLite прошла для числового ключа, текстового `externalId`
при наличии отдельного `id` и отсутствующей записи. `pnpm check-types` прошёл.
Для следующего шага `save` выполнен пример существующего INSERT builder:
вход `{ name: 'Anna' }` вернул через `RETURNING` строку с сгенерированным `id = 1` и `age = 18` из SQL DEFAULT.
E3.4 завершён и закреплён в `src/query/make-repository.test.ts`. Семь тестов публичного интерфейса проверяют:
CRUD, точные строки и отсутствие совпадений, пустой и nullable-фильтры, сохранность соседних строк,
числовой и текстовый первичный ключ, отсутствие PK, сохранение ошибки драйвера и дефект пустого INSERT RETURNING.
Проверки типов запрещают неверный ключ, изменение PK, неверное условие и отсутствие обязательного INSERT-поля.
`pnpm check-types` прошёл; три query test-файла — 22 теста прошли; oxlint изменённых файлов — без замечаний.
Старые закомментированные определения типов удалены. Подробности — [запись 0044](records/0044-e3-4-repository-contract.md).
Пользователь написал Repository с помощью; постоянные тесты написал наставник по явному разрешению.
Самостоятельное восстановление сложных типов и Effect-переходов не подтверждено.

Исходный сценарий E3.5 возвращал для `enabled` числа `1`/`0`; после реализации и
постоянных проверок SELECT возвращает декодированные boolean-значения.

Упражнение E3.2 закрыто целиком. L3.22 подтверждён: условный тип в `col` наблюдается через тип строки результата, две
мутации ломают ровно по одному утверждению, объединение `string | null | null` нормализуется. Подробности — в
[записи 0041](records/0041-l3-22-source-nullability-in-col.md).

Закрытые шаги урока 3 и записи с подробностями:

| шаг                 | результат                                                                          | запись                                                                                                                                                               |
| ------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L3.0–L3.9           | границы пайплайна, FSM, SourceMap, `col`, immutable state, projection              | [0029](records/0029-l3-0-boundary-model-confirmed.md), [0030](records/0030-l3-fsm-sourcemap-contextual-col.md), [0031](records/0031-l3-5-immutable-builder-state.md) |
| L3.10–L3.11         | `ExecutableQuery<R>`, `execute()` через typed `run`                                | [0032](records/0032-l3-10-executable-query-boundary.md), [0033](records/0033-l3-11-execute-delegates-to-typed-run.md)                                                |
| L3.12               | три cardinality terminals и тесты с мутационной проверкой                          | [0034](records/0034-l3-12-cardinality-terminals.md), [0035](records/0035-l3-12-terminals-test-and-mutation-check.md)                                                 |
| L3.14, L3.16, L3.17 | `innerJoin`: рост SourceMap, ON в расширенном контексте, JoinIR                    | [0036](records/0036-l3-14-inner-join-source-extension.md)                                                                                                            |
| L3.15               | политика повторного alias — документация вместо запрета                            | [0037](records/0037-l3-15-duplicate-alias-policy.md)                                                                                                                 |
| —                   | `leftJoin` написан пользователем вне очереди; тип ещё не выражает `null`           | [0038](records/0038-l3-left-join-runtime-null.md)                                                                                                                    |
| L3.13 (E3.1)        | `selectAll()` только при одном источнике, ограничение через `this`                 | [0039](records/0039-e3-1-select-all-single-source.md)                                                                                                                |
| L3.20–L3.21         | `SourceMap` хранит `Source<T, N>`; флаги источников проверены типами               | [0040](records/0040-l3-21-source-nullability-metadata.md)                                                                                                            |
| L3.22 (E3.2)        | nullability источника видна в типе строки результата; E3.2 закрыто                 | [0041](records/0041-l3-22-source-nullability-in-col.md)                                                                                                              |
| L3.24 (E3.3)        | INSERT FSM без `returning`, тип и значение результата, мутационная проверка        | [0042](records/0042-l3-24-insert-fsm.md)                                                                                                                             |
| L3.25 (E3.3)        | Практика multi-row INSERT подтверждена; самостоятельное объяснение остаётся долгом | [0043](records/0043-l3-25-multirow-insert.md)                                                                                                                        |
| E3.4                | Repository проверен через публичный интерфейс и типовые запреты                    | [0044](records/0044-e3-4-repository-contract.md)                                                                                                                     |

Самостоятельные шаги L3.18 (две колонки `id` под разными ключами) и L3.19 (второй JOIN)
остаются долгом. Тест E3.5 проверяет первый случай через INNER/LEFT self JOIN, но написан агентом.

## Что уже реализовано

До урока 3: schema values и inference, tagged Driver errors, диалекты PostgreSQL/SQLite, драйверы PGlite,
better-sqlite3 и libSQL, runtime AST/IR со smart constructors, компилятор, pretty printer и predicate optimizer, typed
expressions/statements и typed `run`, compile cache, Request batching, typed Stream, cross-cutting Driver layers
(tracing, metrics, retry, timeout). Streaming реально работает только в PGlite и better-sqlite3.

Builder урока 3: `selectFrom(table, alias)` создаёт immutable `SelectQueryBuilder<S>` с runtime `BuilderState` и
type-only `SourceMap`; `where` накапливает предикаты через `And`, `orderBy` дополняет список критериев, `limit` и
`offset` заменяют собственные поля; `innerJoin` и `leftJoin` расширяют `SourceMap` пересечением и добавляют один `Join`
в IR; запись `SourceMap` — это `Source<T, N>` с признаком nullability, `false` у `selectFrom` и `innerJoin`, `true` у
`leftJoin`; `select(callback)` переводит запрос в `ExecutableQuery<R>` с `toIR()`, `execute()`, `executeOne()` и
`executeOneOrThrow()`. Контракты terminals покрыты `src/query/builder.test.ts`.

L3.24 закрыт: пользователь реализовал `insertInto`, `InsertQueryBuilder.values` и `ExecutableInsert.execute` в
`src/query/write-builders.ts`. `toIR` оставлен по его решению. Проверены SQL и параметры, отложенное обращение к
драйверу, точный результат `{ affectedRows: 1 }`, тип результата и отсутствие `execute` до `values`.
Пользователь сообщил об ошибке теста после мутации `returning: '*'` и восстановил исходный код.
После восстановления `pnpm check-types` и девять тестов двух builder-файлов прошли.
Подробности — [0042](records/0042-l3-24-insert-fsm.md). Самостоятельное объяснение связи `R` и `returning`
ещё не подтверждено; уровень освоения не повышен.

## Известные технологии

Под «известные» здесь понимается присутствие в практическом коде, а не гарантированный mastery.

- strict TypeScript 7, ESM/NodeNext, package import maps;
- Effect 4 RC: Effect, Context Service, Layer, Scope, Stream, Request/RequestResolver, Schedule, Metric, Match;
- PostgreSQL semantics через PGlite;
- SQLite через better-sqlite3 и libSQL;
- Vitest и `@effect/vitest`;
- pnpm, tsgo language service, oxlint, Prettier, Husky/lint-staged.

## Оценка mastery

| Концепция                                      | Уровень | Основание                                                                                                                                                                   |
| ---------------------------------------------- | ------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Strict TypeScript: generics, unions, narrowing |       3 | Пользователь реализовывал typed AST/optimizer и исправлял type errors с помощью.                                                                                            |
| Conditional и mapped types                     |       3 | Пользователь реализовал single-source mapped type с literal alias и negative cases с помощью.                                                                               |
| Phantom marker / type erasure                  |       3 | Пользователь объяснил, что `R` не участвует в разрешении методов, и реализовал `ExecutableQuery<R>`; запись 0032.                                                           |
| Runtime AST и discriminated unions             |       3 | Подтверждены чтение вложенного дерева и реализация optimizer rules.                                                                                                         |
| Pure compiler `AST + Dialect → SQL + params`   |       3 | Compiler и расширения проверялись с помощью; самостоятельное восстановление целого pipeline ещё нужно проверить.                                                            |
| SQL parameterization и identifier quoting      |       2 | Используется во многих заданиях; системное объяснение threat boundary не зафиксировано.                                                                                     |
| Базовый SQL DDL/DML                            |       1 | Пользователь явно сообщал, что почти не знает БД; VALUES/recursive CTE освоены локально.                                                                                    |
| SQL JOIN и aliases                             |       2 | Прогноз на изменённых данных совпал с наблюдением; оба следствия названы без подсказки, запись 0036.                                                                        |
| Effect lazy execution и `yield*`               |       3 | Пользователь уточнил границу: `execute()` не читает `Driver`, требование объявлено в типе; запись 0033.                                                                     |
| Effect services/Layers и dependency channel    |       3 | Driver context и `provideContext` применялись с помощью в E2.6.                                                                                                             |
| Typed errors и `catchTag`                      |       3 | Пользователь реализовал три cardinality contract с ошибками в error channel; канал успеха путался, запись 0034.                                                             |
| Scope/Fiber/resource lifetime                  |       2 | Интерактивный материал пройден, но объяснение пользователя не подтверждено record.                                                                                          |
| Effect Request batching                        |       3 | Реализован и проверен one-to-many resolver с одним SQL batch.                                                                                                               |
| Effect Stream/backpressure                     |       3 | Typed adapter и миллионный fold реализованы с помощью; producer/consumer boundary объяснена.                                                                                |
| Memory retention: heapUsed/RSS                 |       3 | Пользователь правильно объяснил forced-GC контрпример; record 0028.                                                                                                         |
| Behavioral testing                             |       3 | Написал семь случаев контрактов с fake Driver; состав утверждений и связь calls/строк потребовали разбора.                                                                  |
| Type-level API testing                         |       3 | Compile-only positive/negative FSM cases подтверждены через `@ts-expect-error` и `pnpm check-types`.                                                                        |
| Мутационная проверка тестов                    |       2 | Метод показан агентом: зелёный набор пропускал неверный `count`, пока код не сломали намеренно.                                                                             |
| Сужение типа и `noUncheckedIndexedAccess`      |       2 | Проверка длины не сужает элемент; объяснено на изолированном примере, самостоятельно не восстановлено.                                                                      |
| Непроверенные приведения как источник дефектов |       2 | Форма результата ломалась трижды при молчащем `tsc`; сужение приведения до одного значения показано агентом.                                                                |
| Fluent immutable builder                       |       3 | Пользователь реализовал фабрику и modifier с помощью; initial state и независимость веток подтверждены временным проверочным скриптом.                                      |
| Type-state FSM через class surfaces            |       3 | Пользователь перенёс FSM из compile-only упражнения в runtime API и объяснил причину запрета; запись 0032.                                                                  |
| `exactOptionalPropertyTypes` и сборка объекта  |       2 | Правка выполнена после объяснения на literal-примере; самостоятельное объяснение не подтверждено.                                                                           |
| Ограничение вызова через `this` parameter      |       2 | Метод реализован, но условный тип `this` был показан целиком после трёх итераций; запись 0039.                                                                              |
| Распределение условных типов по объединению    |       1 | Приём `IsUnion` разобран на подстановках и наблюдаемой таблице; самостоятельного вывода не было.                                                                            |
| SourceMap через intersection types             |       3 | `innerJoin` реализован по списку ограничений; расширение alias и два negative type case подтверждены, запись 0036.                                                          |
| LEFT JOIN nullability                          |       3 | Условный тип и три утверждения на тип строки написаны пользователем и подтверждены двумя мутациями; форма `SourceMap` до этого исправлялась четыре раза. Записи 0040, 0041. |
| Граница типа против конкретного типа           |       2 | Различие constraint и аннотации разобрано на `nullable: boolean` против литералов; подстановка `Record<string, Source<...>>` самостоятельно не восстановлена.               |
| Чтение ошибок компилятора как диагностика      |       2 | Приём «развернуть индексируемый тип по цепочке объявлений при `TS2536`» показан наставником; молчание `check-types` при расходящемся `on` пользователем не замечено.        |
| Repository / Data Mapper boundary              |       1 | Термины присутствуют только в курсе; проект ещё не создаёт domain entities.                                                                                                 |
| Transactions/savepoints                        | unknown | Реализации и подтверждённой практики нет.                                                                                                                                   |
| Identity Map / Unit of Work                    |       0 | В текущем коде отсутствуют.                                                                                                                                                 |
| Migrations                                     |       0 | В текущем коде отсутствуют.                                                                                                                                                 |
| Public package design/build consumption        |       1 | package metadata есть, но `src/index.ts` отсутствует при export на `dist/index.*`.                                                                                          |

## Концепции, которые считаются знакомыми, но требуют retrieval check

- AST как данные и `_tag` narrowing.
- Отложенное исполнение Effect.
- Driver в environment channel.
- Typed query как compile-time promise поверх raw rows.
- Stream producer/adapter/consumer и постоянный fold accumulator.

Эти темы не нужно преподавать заново. Сначала короткий прогноз или самостоятельное восстановление; объяснение
добавляется только по обнаруженному пробелу.

## Непроверенные концепции урока 3

- сохранение alias literal без widening;
- indexed access `S[A]['table']['_columns'][C]`;
- projection inference `Expr<T> → T`;
- alias collisions;
- cardinality semantics `execute`/`executeOne`/strict exactly-one.

Признак nullability источника проверен и на уровне метаданных (запись 0040), и на уровне типа строки результата
(запись 0041).

## Ближайшая учебная цель

Практическая часть L3.25 подтверждена: SQL и параметры, отказ при разных наборах колонок, настоящий SQLite,
точные сохранённые строки и `affectedRows: 2`. Очистка теста завершена. `pnpm check-types` — код `0`,
12 тестов двух builder-файлов прошли. Самостоятельное объяснение порядка параметров остаётся учебным долгом;
уровень освоения не повышен. Подробности — [0043](records/0043-l3-25-multirow-insert.md).

Практический результат L3.26 подтверждён: `returning` возвращает выбранные поля на SQLite, тип результата
проверен на реальной цепочке, неизвестная колонка запрещена, исходный запрос сохраняет прежнее поведение.
Пользователь объяснил последствие изменения исходного запроса. Последние проверки: `pnpm check-types` — код `0`,
15 тестов обоих builder-файлов прошли. Мутация `returning: '*'` не выполнена; по повторной просьбе пользователя
двигаемся дальше с этим долгом. Самостоятельное объяснение const generic и распределения условного типа не подтверждено.

L3.27 реализован: `update(table).set(patch)`, `where` с контекстом одной таблицы и накоплением условий через `And`,
`returning`, `toIR`, `execute`. Временные сценарии на SQLite подтвердили результат без WHERE, выбранные поля
с `returning` и изменение только одной из двух строк с двумя условиями. `pnpm check-types` прошёл.
Постоянных тестов UPDATE ещё нет; обработка пустого SET остаётся открытой. Полное завершение L3.27 не заявлено.

L3.28 реализован: `ExecutableDelete<T, R>` и `deleteFrom(table)`. `pnpm check-types` прошёл.
Временный сценарий SQLite подтвердил два условия, выбранные поля удалённой строки, сохранение остальных строк,
пустой результат при повторном удалении, `affectedRows: 1`/`0`, независимость производных запросов и удаление без WHERE.
Текущая граница — общий этап постоянных тестов UPDATE и DELETE. Самостоятельное понимание параметров типа
статического метода не подтверждено: различие разобрано после ошибки, исправление выполнено с помощью.

## Текущие риски проекта, не являющиеся оценкой пользователя

- `package.json` экспортирует `dist/index.js`/`dist/index.d.ts`, но `src/index.ts` сейчас отсутствует.
- `libsql.executeStream` возвращает `Stream.empty`, то есть capability выглядит реализованной, хотя является no-op.
- `typed-run` и typed stream используют unchecked raw-row assertion; runtime schema validation отсутствует.
- Cross-cutting layers в текущем виде просто пробрасывают `executeStream`, поэтому tracing/metrics/retry behavior
  потоковых запросов не эквивалентен `executeRaw`.
- Смысл поля `count` в `TooManyError` не зафиксирован ни в типе, ни в документации.
- Граница FSM защищена только на уровне типов: `src/hw/e3-1.ts` и `src/hw/e3-2.test.ts` проверяются `pnpm check-types`,
  исполнение INNER/LEFT self JOIN дополнительно покрыто тестом E3.5 в `src/query/builder.test.ts`.
- `pnpm lint` даёт три предупреждения о неиспользуемых импортах в `src/query/expression-builder.ts` — остатки перехода
  на `AnyTableDef`.
