# Продолжение ревью ORM

Обновлено: 2026-09-17. Этот файл хранит актуальное состояние девяти пунктов ревью пользователя.
Код и проверки имеют приоритет над записями. Старые номера строк из исходного ревью не считать актуальными.

## Кодовая фраза и точка продолжения

**Кодовая фраза: «Продолжаем ревью ORM по списку».**

При этой фразе прочитать этот файл и `learning/session.md`. Не начинать ревью заново и не выдавать уже
выполненные boolean/Date/JSON-сценарии как новые задания. После каждого законченного пункта обновлять статус,
проверки и следующий шаг здесь по запросу пользователя о фиксации прогресса.

Пункт 5 — безопасность `RetryLayer` — закрыт в рамках явного разрешения `canRetry` на уровне вызова.
Пункт 7 — пул PGlite — закрыт в рамках контракта ниже. Точка продолжения: **пункт 8 — оптимизатор и кэш компиляции**.
Пункт 6 отложен по решению пользователя: настоящий таймаут SQLite через `better-sqlite3` требует
нативного модуля на C и отдельной сборки. Таймаут PGlite также отложен. Передача `timeoutMs`
сохранена, но отмена SQL в драйверах ещё не реализована. Эксперимент и ограничения — в разделе 6.

Нумерацию исходного списка сохранять. Пункт 6 отложен, пункт 7 завершён; следующий по списку — пункт 8.
Не подменять весь список только ближайшей задачей.

## Рабочий режим

- Backend mentoring активен. Backend-код, конфигурацию и постоянные тесты пишет пользователь.
- В этой сессии пользователь попросил больше самостоятельности и меньше конкретных указаний кода.
  Объяснять цель и ограничения; подробные подсказки давать по запросу, без затянутого опроса.
- Пользователь просил исключение для полных тестов, но session-level отключения mentoring не было.
  Следовать актуальным правилам проекта, не считать это разрешением писать backend за пользователя.
- Пользователь хочет следовать уроку:
  `/home/vyacheslav/Documents/bat-school/orm/BatSchool · Своя ORM на TypeScript + Effect.ts.html`.
- В §1.4 урока Date хранится в PostgreSQL как TIMESTAMPTZ, JSON как JSONB; SQLite использует TEXT.
  Примеры самого урока несогласованны: конструкторы timestamp/json там оставляют `_type: 'text'`.
  Это противоречие уже выявлено и объяснено; мы исправили метаданные и диалекты по заявленному контракту,
  а не заменили PostgreSQL-хранение на TEXT. Не предлагать снова единое TEXT-хранение без обсуждения.

## 1. Кодирование записи и условий — основное исправлено, остаток открыт

### Закрыто в проверенных сценариях

- `INSERT`: encode значений, пропуск null, сохранность исходного IR, CodecError с исходной причиной,
  колонкой и доменным значением; при ошибке кодека запись отсутствует.
- Boolean round-trip через собственные insert/select builders на SQLite.
- `repo.save`: boolean и JSON, включая декодирование INSERT RETURNING.
- `SELECT WHERE`: Eq/Neq/сравнения порядка, And/Or/Not, In, Between обрабатываются encodePredicate.
  Boolean findBy(true/false) и ошибка WHERE-кодека закреплены постоянными тестами.
- `JOIN ON`: кодирование подключено независимо от наличия WHERE. Постоянный тест
  `encodes JOIN ON and WHERE together` проверяет оба условия вместе; исправлена потеря подготовленного WHERE.
- `UPDATE SET`: encode через общий encodeRow; `UPDATE WHERE`: передаётся sources, выполняется encodePredicate.
- `UPDATE RETURNING`: декодирование подключено. Тест репозитория проверяет false → 0 → false,
  точное SQL-хранение и неизменность соседней строки.
- `DELETE WHERE`: boolean-условие кодируется; соседняя строка сохраняется.
- `DELETE RETURNING`: выбранные id/active возвращаются с boolean, а не SQL-числом.
- `write-builders.ts`: сбор фабрик RETURNING вынесен в общую внутреннюю функцию для INSERT/UPDATE/DELETE.
- `typed-run.ts`: выделены getCodec, encodeLiteral, prepareCodecs, encodePredicate, encodeRow.
  Выбор кодека получает dialectId; encodeRow не меняет входной объект.

### Остаток и ограничения доказательств

- **LIKE:** контракт не определён. SQL-шаблоны `%`/`_` нельзя автоматически кодировать как обычное доменное
  значение: произвольный кодек может разрушить смысл шаблона. Не добавлять тег Like в общую ветку без решения.
- **Потоковый путь** `src/query/typed-stream.ts` компилирует Select отдельно от runWithSql и не использует
  описанную подготовку/декодирование. Не объявлять кодеки всех способов исполнения закрытыми.
- При рефакторинге была реальная регрессия Not: рекурсия получала сам pred вместо pred.pred и зависала.
  Исправлено. Временный SQLite-сценарий подтвердил обычное сравнение и NOT с точными разными строками;
  **постоянного регрессионного теста NOT в этой сессии не добавлено**. 38 прежних тестов не ловили зависание.
- Основные UPDATE/DELETE/JOIN boolean-сценарии проверены на SQLite, не все варианты на PostgreSQL.
  Множественные JOIN, полный набор кодеков/предикатов, ошибки/NULL во всех write-вариантах не доказаны.
- Низкоуровневые вызовы без метаданных таблицы не получают автоматически гарантии typed builders.

Уточнение исходного ревью: IR может хранить доменные литералы — encode выполняется перед compile, когда
известен диалект. SQLite-ошибка уже оборачивалась в типизированный DbError; утверждение «не типизированная»
неточно. Общий контракт должен иметь round-trip через свой API, но отдельный тест decode/WHERE вправе
использовать сырой SQL для независимой подготовки данных.

Основные файлы: `src/query/typed-run.ts`, `src/query/write-builders.ts`,
`src/query/builder.test.ts`, `src/query/write-builder.test.ts`, `src/query/make-repository.test.ts`.

## 2. Доменные типы кодеков — исходные дефекты исправлены

- withCodec сохраняет обязательную фабрику Codec<TS, SQL>; InferValue извлекает TS, InferColumn добавляет null.
  Дополнительный фантом `_ts` не вводили: существующей фабрики достаточно для вывода типа.
- json<T>() сохраняет T. timestamp() больше не имеет стирающей аннотации ColumnDef<'text'>.
- SqlType расширен `timestamp` и `json`; соответствующие конструкторы используют эти метаданные.
- Диалекты: timestamp → TIMESTAMPTZ / TEXT; json → JSONB / TEXT (PostgreSQL / SQLite).
- SqlToTs содержит timestamp: Date и json: unknown. Последнее — безопасный запасной тип; json<T>() выводит T
  через обязательную фабрику кодека, а не превращается в unknown.
- dateCodec/jsonCodec сохранили PostgreSQL passthrough и SQLite преобразования. Тип SQL теперь соответствует им.
- `preserves Date through save and findById`: фиксированный Date, SQL-тип из описания схемы, проверены
  INSERT RETURNING и findById на SQLite и PGlite.
- `encodes JSON on save and decodes the returned row`: объект `{ language: 'ru' }` через save и findById
  на SQLite и PGlite. Сырое хранение отдельно: SQLite — JSON-строка, PGlite JSONB — объект.
- Тест сначала оставлял mapCol('text') для PostgreSQL и получил '[object Object]'. Исправлен DDL теста:
  тип берётся из `_columns.payload._type`, ожидания не подгонялись под потерю данных.

Ограничения: PostgreSQL проверен через PGlite, не отдельный сервер. Generic T — не runtime-валидация JSON.
Не объявлять проверенными все допустимые JSON-значения, invalid Date, временные зоны и точность сверх Date.

Файлы: `src/schema/columns.ts`, `src/schema/infer.ts`, `src/dialect.ts`, `src/codec.ts`,
`src/query/make-repository.test.ts`.

## 3. PGlite streaming с параметрами — открыт

`src/drivers/pglite.ts`, executeStream: DECLARE выполняется через pg.exec без params; params передаются
в FETCH. Установленный API exec не принимает массив параметров, query принимает.
`src/query/typed-stream.test.ts` не доказывает PGlite streaming с параметрами.
Это подтверждено чтением кода/API; отдельный runtime-прогон этой ошибки в текущей сессии не выполнялся.
Следующий критерий: реальный потоковый запрос PGlite с параметром, точные строки, корректное освобождение
курсора при завершении/ошибке/прерывании. Не считать простой запрос без параметров достаточной проверкой.

## 4. Слои не обрабатывают executeStream — открыт

`src/layers/retry.ts`, `metric.ts`, `tracing.ts`, `slow-query-log.ts`, `statement-timeout.ts`
напрямую передают inner.executeStream. Исправлений не было.
Нужно определить поведение каждого слоя для жизненного цикла потребления потока, а не только его создания.
Не копировать retry вслепую: повтор после выдачи части строк может дублировать результат.

## 5. RetryLayer и неидемпотентная запись — закрыт

- `DriverImpl.executeRaw` принимает необязательный третий аргумент с `canRetry`.
  Без `canRetry: true` выполняется одна попытка. Разрешение означает, что вызывающий код гарантирует
  безопасность повторного выполнения, даже если первая попытка уже применила изменение.
- `RetryLayer` дополнительно проверяет временный характер ошибки и соблюдает `maxAttempts`.
  `ConnectionFailureError`, `StatementTimeoutError` и `DatabaseBusyError` сами по себе не разрешают повтор.
- `MetricLayer`, `SlowQueryLogLayer`, `StatementTimeoutLayer` и `TracingLayer` передают опции дальше.
- Постоянный тест потери ответа моделирует списание до ошибки: без разрешения наружу возвращается
  `ConnectionFailureError`, баланс остаётся `900`, а не `800`.
- Постоянные тесты проверяют лимит разрешённых повторов и отсутствие повторов постоянной ошибки
  даже при `canRetry: true`. Сценарий `DatabaseBusyError` проходит через внешний `MetricLayer`:
  потеря опций уменьшит число попыток с трёх до одной и приведёт к падению теста.
- Проверки: `pnpm test src/layers` — 10/10; `pnpm check-types` — без ошибок.
  Временный сценарий через каждую из четырёх внешних обёрток подтвердил две попытки и успех
  после первой временной ошибки при `canRetry: true`. Для остальных трёх обёрток постоянного
  теста сочетания с `RetryLayer` нет. Общий набор тестов проекта не запускался.

## 6. Таймаут и отмена запроса — отложен

### Решение пользователя и границы

Последнее решение пользователя: отложить настоящий таймаут SQLite после объяснения необходимости
писать нативный модуль на C и собирать отдельную библиотеку. Таймаут PGlite также остаётся отложенным.
Продолжаем пункт 7; новый драйвер серверного PostgreSQL не нужен.

Сохранённая цель при возврате к пункту 6 — остановка выполняющегося SQL с сохранением работоспособного
соединения и базы `:memory:`, а не только возврат ошибки по таймеру.

Backend mentoring остаётся активным: пользователь пишет backend, нативный код, конфигурацию и постоянные
тесты. Агент исследует, объясняет и запускает проверки. Пользователь просит самостоятельность:
объяснять цель и ограничения, давать один содержательный следующий шаг, без готовой реализации и
без последовательности мелких синтаксических указаний.

### Уже сделано пользователем

- `src/drivers/driver.ts`: `ExecuteRawOptions` содержит необязательные `canRetry` и `timeoutMs`.
- `src/layers/statement-timeout.ts`: внутренний вызов получает `{ ...options, timeoutMs }`.
  Настройка слоя имеет приоритет; клиентская гонка `Effect.raceFirst` пока сохранена.
- `src/layers/retry.ts`: внутреннему драйверу теперь передаётся весь `options`.
- `pnpm check-types` прошёл после последней правки слоя.
- `pnpm exec vitest run --config vitest.config.ts src/layers/retry.test.ts` — 4/4 перед последней
  правкой слоя. Временный сценарий после неё проверил цепочку
  `StatementTimeoutLayer → RetryLayer → Driver`: без опций получен `{ timeoutMs: 200 }`,
  с `{ canRetry: true, timeoutMs: 999 }` получен `{ canRetry: true, timeoutMs: 200 }`.
  Это доказательство передачи параметров, не отмены SQL.
- `src/drivers/sqlite.ts` ещё не использует `timeoutMs`. `SQLITE_INTERRUPT` сейчас превратится
  в общий `DbError`; преобразование ошибки дедлайна в `StatementTimeoutError` не реализовано.

### Подтверждённые механизмы SQLite

Установлен `better-sqlite3 13.0.3`. Его синхронный вызов внутри `Effect.try` блокирует поток:
JS-таймер не может прервать запрос. Параметр `timeout` библиотеки задаёт `sqlite3_busy_timeout`
для ожидания блокировки, а не предельную длительность SQL.

Первый временный сценарий с реальным SQLite и `StatementTimeoutLayer({ timeoutMs: 1 })` дал
`Success` за 159 мс: сумма рекурсивного ряда 1..1000000 равна `500000500000`.
Это известное воспроизведение; не повторять ради подтверждения уже установленного факта.

**Проверен рабочий механизм без замены `better-sqlite3`:**

- В отдельном процессе Node создана база `:memory:`, таблица `accounts(id, balance)`, строка `(1, 1000)`.
- Через `db.loadExtension` загружено одноразовое C-расширение. Оно получило `sqlite3*` через
  `sqlite3_extension_init`, запустило `pthread`, подождало 50 мс через `nanosleep` и вызвало
  `sqlite3_interrupt(db)`. Перед закрытием БД поток был завершён через `pthread_join`.
- Выполнен вычислительно тяжёлый SQL:

  ```sql
  UPDATE accounts SET balance = balance - 100
  WHERE id = 1 AND (
    WITH RECURSIVE n(x) AS (
      VALUES(1) UNION ALL SELECT x + 1 FROM n WHERE x < 10000000
    )
    SELECT sum(x) FROM n
  ) > 0
  ```

- Наблюдение: `SQLITE_INTERRUPT`, 50 мс, баланс `1000`, последующий `SELECT 42` успешен.
  JS-таймер на 10 мс к моменту возврата ещё не сработал. Прерывание пришло из нативного потока.
- Расширение собиралось `cc -shared -fPIC -O2 -pthread` с заголовками из
  `node_modules/better-sqlite3/deps/sqlite3`. На Linux компилятор доступен.
  Все файлы эксперимента из `/tmp` удалены; готового расширения в репозитории нет.
  Эксперимент был одноразовым, с фиксированным таймером и одним соединением, не производственной реализацией.

**Отвергнутый путь на штатной сборке:** `sqlite3_progress_handler` отключён опцией
`SQLITE_OMIT_PROGRESS_CALLBACK` в `node_modules/better-sqlite3/deps/defines.gypi`.
Это подтверждено `PRAGMA compile_options`, также показавшей `THREADSAFE=2`.
Пробный вызов отсутствующего обработчика через расширение завершил отдельный Node-процесс кодом 139.
Не повторять небезопасный эксперимент. Для этого альтернативного механизма нужна другая сборка SQLite.

Первичные источники:
[sqlite3_interrupt](https://www.sqlite.org/c3ref/interrupt.html),
[sqlite3_progress_handler](https://www.sqlite.org/c3ref/progress_handler.html).
`sqlite3_interrupt` допускается вызывать из другого потока, но соединение должно оставаться открытым.
Прерывание почти завершённого запроса может прийти слишком поздно. При прерывании записи внутри
явной транзакции SQLite может откатить всю транзакцию; не обещать универсальный откат только одного statement.

### При возврате к отложенному пункту и критерии

1. Прочитать актуальные `src/drivers/sqlite.ts`, `src/drivers/driver.ts`,
   `src/layers/statement-timeout.ts` и конфигурацию сборки. Повторно исследовать доступность API не нужно,
   если версии не изменились.
2. Граница TypeScript → нативное расширение → `sqlite3_interrupt` уже объяснена.
   Обсуждена гарантия снятия дедлайна: после возврата невозможен запоздалый вызов прерывания.
   Минимальный интерфейс и синхронизация ещё не определены; начинать с жизненного цикла, не полного C-кода.
3. Разработать расширение и интеграцию: состояние принадлежит соединению; дедлайн использует монотонное
   время; завершение запроса снимает таймер; закрытие дожидается потока отмены; запоздалый таймер
   не прерывает следующий запрос. Одноразовый глобальный таймер из эксперимента копировать нельзя.
4. Проверить через ORM: медленная запись прервана с `StatementTimeoutError`, баланс не изменён,
   соединение пригодно для следующего запроса; быстрый запрос не вызывает запоздалой отмены следующего;
   несколько соединений не отменяют запросы друг друга; закрытие безопасно.

Нативная сборка/упаковка, поддерживаемые платформы и окончательный интерфейс расширения ещё не определены.
Не считать сборку только на Linux доказательством переносимости. SQL-функции управления расширением,
если будут выбраны, не должны становиться способом обхода дедлайна в недоверенном SQL.
Синхронный SQL даже с нативным дедлайном продолжит блокировать JS до завершения/прерывания.
Произвольное внешнее прерывание Effect в это время отдельно не доказано; перенос в Worker — отдельное
архитектурное решение, не автоматическое следствие таймаута.
Отмена не даёт гарантии «любая ошибка таймаута означает отсутствие записи», если запись успела
завершиться до обработки отмены. Для регрессии использовать явно долгую операцию, не гонку около дедлайна.

### PGlite и состояние тестов — для сохранения контекста

PGlite `0.5.7` не предоставляет публичный `AbortSignal`/метод отмены запроса.
С `statement_timeout = 10ms` медленный `UPDATE` через `pg_sleep(0.1)` завершился успешно за 106 мс
через ORM; баланс стал `900`. Прямой опыт с серверной настройкой дал успех за 101 мс.
Вычислительный `UPDATE` с рекурсивным рядом 1..1000000 также успешно завершился за 589 мс, баланс `900`.
Синхронный WASM блокирует обработку JS-таймеров. Нужен отдельный прототип механизма внутри WASM;
его работоспособность не доказана. Уничтожение Worker уничтожает экземпляр, а не заменяет штатную отмену.

В `src/layers/statement-timeout.test.ts` остался новый реальный тест на PGlite
`cancels a slow write without changing the balance`: последняя проверка дала 2 успешных теста и 1 отказ.
Ожидалась ошибка таймаута, получен `Result.Success(undefined)`; проверка баланса после assertion не дошла.
Не выдавать этот тест за тест серверного PostgreSQL и не подгонять ожидания под ошибочное поведение.
Для SQLite требуется собственная регрессия на настоящем драйвере. Полный suite после текущих изменений
не запускался; прежнее «10/10 слоёв» из пункта 5 — исторический результат.

Отдельный временный сервер PostgreSQL тоже проверялся, но пользователь отклонил добавление нового драйвера.
Контейнер удалён. Этот путь не является продолжением задачи; возможности PostgreSQL нельзя приписывать PGlite.

## 7. makePool PGlite — закрыт в рамках E1.8

### Исправление и контракт

В `src/drivers/pglite.ts` пользователь выполняет `make(options)` один раз внутри `Effect.gen`,
до создания `Pool.make`. В `acquire` передаётся `Effect.succeed(impl)`.
Прежний вариант создавал отдельную базу PGlite на каждый ресурс пула.

`makePool(size, options)` создаёт один экземпляр PGlite на одно выполнение эффекта создания пула.
Все ресурсы этого пула возвращают один и тот же `DriverImpl` и обращаются к общей базе.
Разные выполнения `makePool` не обязаны разделять базу.

- `size` задаёт максимальное число одновременно выданных ресурсов, а не число WASM-экземпляров.
- `Pool.get` удерживает ресурс до закрытия области потребителя (`Scope`).
  Когда все ресурсы заняты, следующий потребитель ждёт возврата ресурса.
- Возврат отдельного ресурса не закрывает общую базу. База принадлежит области, в которой создан пул.
- Потребители должны закончить работу и вернуть ресурсы до закрытия области владельца пула.
  Нельзя продолжать SQL через выданный драйвер после начала закрытия этой области.
- Несколько ресурсов не создают независимых SQL-сессий, параллельного исполнения SQL в одном
  WASM-экземпляре или изолированных транзакций. `Pool` с `size > 1` сам по себе не сериализует
  последовательности запросов разных потребителей; смешивание транзакций и курсоров этим исправлением
  не решено. Гарантия эксклюзивного владения сессией не заявлена.
- Ограничение `size` действует при получении через `Pool.get`; оно не ограничивает произвольные
  конкурентные вызовы через уже полученный `DriverImpl`.

Это трактовка E1.8 для одного WASM-экземпляра: ограниченный доступ к общей базе,
а не обычный пул независимых соединений серверного PostgreSQL.
Урок требует `Pool.make` с настраиваемым размером, но пояснение о сериализации через семафор
нельзя читать как гарантию независимых транзакционных сессий.

### Проверки

Исходный дефект воспроизведён с двумя одновременно удерживаемыми ресурсами `makePool(2)`:
первый создавал таблицу и читал `[{ id: 42 }]`, второй получал `42P01` (таблица отсутствует).
После изменения тот же временный сценарий успешно прочитал строку через оба ресурса.

В `src/drivers/pglite-pool.test.ts` сохранены три постоянных теста:

1. Общая таблица доступна двум одновременно удерживаемым ресурсам: создание через первый,
   вставка через второй с проверкой `affectedRows: 1`.
2. После закрытия области потребителя ресурс используется повторно, данные базы сохраняются.
3. При двух занятых ресурсах третий потребитель ждёт; после освобождения второго завершается.
   Получение второго подтверждает `acquired`; третий запускается с `startImmediately: true`,
   затем до `release` проверяется `Deferred.poll(thirdAcquired)` на `None`.

Последние проверки после удаления дополнительного теста закрытия:
`pnpm exec vitest run --config vitest.config.ts src/drivers/pglite-pool.test.ts` — 3/3;
`pnpm check-types` — успешно. Полный набор тестов, линтер и сборка не запускались.
В имени третьего теста остался лишний апостроф; на поведение проверки он не влияет.

### Отозванное расширение задачи

Агент добавил требование ожидать при закрытии пула потребителя из отдельного scope,
не подтвердив такой контракт библиотеки. Четвёртый тест закрывал область владельца базы
до завершения потребителя; SQL зависал, запуск был остановлен через 120 секунд.
В чистом `Pool.make` без БД на Effect `4.0.0-rc.108` и отдельно на `rc.115`
закрытие области владельца завершалось при удерживаемом ресурсе.
Это наблюдение не доказывает нарушение официального контракта Effect.

Предложения удалить `makePool`, патчить Effect и считать проблему подтверждённым багом библиотеки
отозваны. Точного GitHub issue не найдено; связанные #7599 / PR #7604 касаются `Pool.reserve`,
а не этого сценария. Issue не опубликован, патч не создан, зависимости проекта не изменены.
По согласованию с пользователем четвёртый тест удалён, не пропущен.
Патч и дальнейшее расследование закрытия не являются следующими заданиями.

### Учебный контекст

Пользователь просит небольшие итерации с объяснением Fiber/Deferred/Scope.
В квизе 5/6 ответов верны; первая ошибка — предположение, что завершить `Deferred` до ожидания нельзя.
Объяснено сохранение результата. Тест ожидания написан с подсказками; самостоятельное проектирование
конкурентного теста не подтверждено. Следующий пункт списка — 8.

## 8. Оптимизатор и кэш компиляции — открыт

`optimizeSelect` используется в тестах/benchmark; `makeCompileCache` — в benchmark.
`src/compiler/run.ts`, `src/query/typed-run.ts`, `src/query/typed-stream.ts` вызывают compile напрямую.
Интеграция не выполнена. Сначала безопасность/корректность, затем оптимизация; нужен обоснованный контракт
кэша с учётом диалекта и параметров. Не выдавать отсутствие оптимизации само по себе за неверный результат SQL.

## 9. Остальные пункты — раздельный статус

### 9.1. Синхронные throw — открыт вопрос контракта

`makeRepository` бросает PrimaryKeyError при отсутствии PK; `selectAll` бросает QueryInvariantError при
нарушенном состоянии. Не переведены автоматически в error channel. ReturningError при пустом INSERT RETURNING
уже передаётся через Effect.die; NotFoundError/TooManyError исполнения идут по каналу ошибок.
Сначала решить, где ошибка использования синхронного конструктора, а где ожидаемая ошибка исполнения.
Нельзя механически вызывать Effect.die вместо throw без изменения возвращаемого интерфейса.

### 9.2. Alias из первой буквы — не изменён, низкий приоритет

`src/query/make-repository.ts`: t._name[0] ?? 'r'. Пока это внутренний alias однотабличного запроса,
совпадение между разными репозиториями не является доказанным дефектом. Не создавать задачу о коллизии
разных запросов; пересмотреть правило при реальной необходимости соединять источники.

### 9.3. Undefined в критериях — открыт

findBy/findMany обходят Object.entries(criteria), отдельно учитывают null, но не undefined.
exactOptionalPropertyTypes запрещает обычный типизированный вызов с явным undefined, но runtime-гарантии нет.
Нужно выбрать и проверить контракт: пропуск либо отказ; не менять смысл фильтра молча без решения.

### 9.4. executeOne / executeOneOrThrow — открыт

Весь результат загружается до выбора первой строки/проверки количества.
LIMIT 1/2 не добавлен. При изменении учитывать существующий limit/offset и семантику TooManyError.count:
сейчас поле содержит длину полученного результата; LIMIT 2 не сохраняет точное полное количество.

### 9.5. Nullable-поля в InferInsert — открыт

`src/schema/infer.ts`: необязательными становятся PK/default, но не nullable без default.
Нужно согласовать вывод типа и реальную вставку с пропущенной nullable-колонкой, включая существующие
ограничения набора колонок multi-row INSERT. Одной правки type-level недостаточно для проверки поведения.

### 9.6. Повторный alias JOIN — открыт

innerJoin/leftJoin не запрещают повторный alias, есть только предупреждающий комментарий.
Поправка к исходной рекомендации: возвращаемый тип never сам по себе НЕ запрещает вызов;
ограничение нужно на допустимом аргументе. Runtime-защита нужна для динамических имён/нетипизированных вызовов.

### 9.7. toIR через spread — частично защищён, остаток открыт

`ExecutableQuery.toIR` исключает sources/codecFactories деструктуризацией, затем использует ...rest.
Текущая утечка этих двух полей устранена, но явного построения только IR-полей нет: будущее поле состояния
может попасть в IR. Не отмечать исходный риск полностью закрытым.

## Проверки на момент паузы

- После UPDATE/DELETE и их рефакторинга: 40 тестов трёх query-файлов прошли, check-types прошёл.
- После timestamp: `pnpm test src/query/make-repository.test.ts src/query/builder.test.ts src/query/write-builder.test.ts`
  — **41/41**, `pnpm check-types` — код 0.
- После последней правки JSON с findById:
  `pnpm exec vitest run --config vitest.config.ts src/query/make-repository.test.ts -t 'encodes JSON on save and decodes the returned row'`
  — **1 прошёл, 12 пропущены**, внутри сценария оба слоя; `pnpm check-types` — код 0.
- Полные три файла после последней JSON-правки повторно не запускались. Не называть прежние 41/41 новым запуском.
- Линтер, сборка и полный suite проекта в заключительной части не запускались.
- Временная проверка типов подтвердила Date вместо string для timestamp; файл удалён.
- Временные сценарии JOIN/WHERE, NOT, JSON на двух диалектах не заменяют постоянное покрытие всех границ.

## Отдельно известное, не терять при работе со streaming

`src/drivers/libsql.ts` возвращает Stream.empty из executeStream: это незавершённая реализация,
а не подтверждённая поддержка стриминга. Этот долг существовал до текущего списка; в этой сессии не исправлялся.
Транзакции, relations и старые учебные долги автоматически закрытыми не считать.
