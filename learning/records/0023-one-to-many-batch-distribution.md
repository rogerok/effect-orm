# One-to-many batch раздаёт отдельные массивы

Пользователь самостоятельно исправил группировку E2.6: первая строка создаёт группу, последующие строки дополняют её, а отсутствующая группа превращается в `[]`; также добавлен helper одного логического request. Сквозная проверка с тремя конкурентными userId подтвердила один SQL `IN ($1, $2, $3)` и результаты размеров `2, 0, 1`, поэтому следующий учебный шаг — закрепить этот контракт acceptance-тестом.
