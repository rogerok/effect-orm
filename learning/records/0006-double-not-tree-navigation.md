# Двойной Not прочитан как вложенное дерево

Пользователь самостоятельно определил корневой и дочерний `_tag` в объекте `Not(Not(p))` и указал точный возвращаемый узел `input.pred.pred`. Следующий шаг может перейти от чтения AST к пользовательской реализации одной ветки `Not`, пока без остальных правил E2.3.

**Подтверждение:** последовательные ответы пользователя: корень `Not`, ребёнок `Not`, результат `input.pred.pred`.
